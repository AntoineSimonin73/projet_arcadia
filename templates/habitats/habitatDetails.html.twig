<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zoo d'Arcadia</title>
    <link rel="icon" type="image/svg+xml" href="{{ asset('assets/img/logo_zoo.svg') }}">
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hanuman:wght@100;300;400;700;900&family=Happy+Monkey:wght@100;300;400;700;900&display=swap" rel="stylesheet">
</head>

<body>

    {% include 'blocCode/header.html.twig' %}
    {% block body %}
    <section class="animaux">
        <h3>{{ habitat.nom }}</h3>
        <p>{{ habitat.description }}</p>
        <div class="grid_animaux">
            {% for animal in habitat.animaux %}
                <a href="{{ path('app_animal_show', { 'id': animal.id }) }}" class="liens_animaux">
                    <img src="{{ vich_uploader_asset(animal, 'imageFile') }}" class="img_animaux" alt="{{ animal.nom }}">
                    <h3>{{ animal.nom }}</h3>
                </a>
            {% endfor %}
    </section>
    {% endblock %}
    
    {% include 'blocCode/footer.html.twig' %}

</body>
</html>